# Local only — exposes Postgres on host :5435 (5432 is used by other projects)
services:
  postgres:
    ports:
      - "5435:5432"
  api:
    environment:
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:5173,http://localhost:5174,http://localhost}
