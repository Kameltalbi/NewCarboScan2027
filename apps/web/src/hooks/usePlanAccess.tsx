// Re-export from shared to avoid duplicate code
// All imports should use @/shared/hooks/usePlanAccess directly
export { usePlanAccess } from '@/shared/hooks/usePlanAccess';
export type { UserPlan, PlanFeatures } from '@/shared/hooks/usePlanAccess';
